# การมีส่วนร่วมพัฒนา (Contributing)

โปรเจกต์นี้เป็นซอฟต์แวร์ธุรกิจแบบสงวนลิขสิทธิ์ (ดู [LICENSE](./LICENSE)) เอกสารนี้อธิบายขั้นตอนทำงานสำหรับผู้ที่ได้รับอนุญาตให้เข้าถึงและแก้ไขโค้ดนี้

## เริ่มต้นพัฒนา

ทำตามขั้นตอนติดตั้ง/รันโปรเจกต์ในหัวข้อ ["เริ่มต้นใช้งาน" ของ README](./README.md#เริ่มต้นใช้งาน) ก่อน ให้แน่ใจว่ารันได้ทั้งฝั่ง frontend (`npm run dev`) และ backend (`cd server && npm run dev`)

## Branch `main` มีการป้องกัน — ต้องส่งผ่าน Pull Request เท่านั้น

`main` ตั้งค่าบังคับไว้ (บังคับใช้กับทุกคนรวมถึงเจ้าของ repo ด้วย ดูรายละเอียดในหัวข้อ ["การป้องกัน branch `main`" ของ README](./README.md)):

- **ห้าม push เข้า `main` โดยตรง** — ต้องเปิด Pull Request เท่านั้น
- **ต้องผ่าน CI ทั้ง 2 job ก่อน merge ได้เสมอ**: `Frontend (build, lint, test)` และ `Backend (install, syntax check)`
- ห้าม force-push และห้ามลบ branch

### ขั้นตอนส่งการเปลี่ยนแปลง

1. สร้าง branch ใหม่จาก `main` ตั้งชื่อแบบ `ประเภท/คำอธิบายสั้นๆ` เช่น `fix/`, `feat/`, `docs/`, `chore/`, `test/`, `ci/`
   ```bash
   git checkout -b fix/short-description
   ```
2. แก้โค้ด แล้วรันตรวจสอบก่อน commit เสมอ:
   ```bash
   npm run build
   npm run lint
   npm test
   ```
3. Commit โดยอธิบาย **เหตุผล (why)** ที่แก้ ไม่ใช่แค่บอกว่าแก้อะไร (ดูตัวอย่างจาก `git log` ของโปรเจกต์นี้)
4. Push branch แล้วเปิด Pull Request เข้า `main`
5. รอ CI ผ่านทั้ง 2 job ก่อนขอ merge

### ถ้า CI ไม่ผ่าน

- ดู log ที่ failed จากหน้า Actions ของ PR นั้น
- แก้ไขแล้ว commit เพิ่มเข้า branch เดิม push ทับ ไม่ต้องเปิด PR ใหม่
- ถ้าปัญหาดูเหมือนเกิดจากสภาพแวดล้อม (เช่น library ไม่รองรับบาง runtime) ไม่ใช่จากโค้ดที่แก้ ให้ตรวจสอบให้แน่ใจก่อนเปลี่ยนโค้ดจริง — ดูตัวอย่างในหมายเหตุ `.claude/skills/github-pr-workflow/references/troubleshooting.md` ที่เคยเจอปัญหานี้จริง

## มาตรฐานโค้ด

- ใช้ `oxlint` ตรวจสอบโค้ด (`npm run lint`) — ไม่มี auto-formatter บังคับ ให้ยึดสไตล์โค้ดที่มีอยู่แล้วในไฟล์ข้างเคียง
- Comment เฉพาะตอนที่อธิบาย "ทำไม" ที่ไม่ชัดเจนจากโค้ดเอง (เงื่อนไขซ่อนอยู่, workaround เฉพาะเจาะจง) ไม่ต้อง comment อธิบายว่าโค้ดทำอะไรถ้าอ่านจากชื่อตัวแปร/ฟังก์ชันเข้าใจอยู่แล้ว
- ทุกฟีเจอร์ที่มีการ "สร้าง/เพิ่ม" ข้อมูลที่ส่งผลต่อ state อื่น (เช่น เพิ่มสต็อก, บันทึกยอดลูกค้า) ต้องคิดถึงเส้นทางการ "ยกเลิก/ย้อนกลับ" ควบคู่ไปด้วยเสมอ — เป็นบั๊กคลาสที่พบบ่อยที่สุดในโปรเจกต์นี้ (ดู `.claude/skills/audit-and-fix/SKILL.md`)

## ทดสอบ

โปรเจกต์นี้ใช้ Vitest ทดสอบ context providers ที่มี logic ซับซ้อน (โดยเฉพาะคู่ create/reverse) รันด้วย:

```bash
npm test
```

ถ้าเพิ่ม logic ใหม่ที่มีผลข้างเคียงต่อ state อื่น (ทำนองเดียวกับ `recordPurchase`/`reversePurchase`) ควรเพิ่มเทสคู่กันไว้ใน `src/context/__tests__/` ด้วย

## License

โค้ดนี้สงวนลิขสิทธิ์ทั้งหมด การส่ง Pull Request เข้ามาหมายถึงคุณยินยอมให้ผลงานนั้นตกเป็นส่วนหนึ่งของโค้ดที่สงวนลิขสิทธิ์นี้ ตามเงื่อนไขใน [LICENSE](./LICENSE)
